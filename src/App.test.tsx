import React from "react";
import { render,screen } from "@testing-library/react";

test('score is increment when answer is correct', ()=>{
    // const checkAnswer = (e: React.MouseEvent<HTMLButtonElement> ) => {}
    // checkAnswer digunakan oleh component <QuestionCard/>

    // bagaimana cara menguji checkAnswer?
    

    // karena 'checkAnswer' tidak di export, apakah bisa di uji di tempat terpisah?

    // expect()
});